@model List<AnimalModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Animals</h1>

<p>
    <a asp-action="Create">Create new</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Id)
            </th>*@
            <th>
               Spieces
            </th>
            <th>
                Name
            </th>
            <th>
                Age
            </th>
            @*<th>
            @Html.DisplayNameFor(model => model.Description)
        </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (AnimalModel animal in Model)
        {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => animal.Spieces)
            </td>
            <td>
                @Html.DisplayFor(modelItem => animal.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => animal.Age)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>*@
            @*<td>
            @Html.DisplayFor(modelItem => item.Completed)
        </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new {id=animal.Id }) |
                @Html.ActionLink("Details", "Details", new { id = animal.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id= animal.Id })
            </td>
        </tr>
        }
    </tbody>
</table>
